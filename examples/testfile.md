# Markdown Preview

Diese Datei kann in der App hochgeladen werden.

## Warum Container?

Ein Container enthält nicht nur den eigenen Python-Code, sondern auch die benötigte Laufzeitumgebung.
In diesem Beispiel ist vor allem das externe Systemprogramm `pandoc` wichtig.

## Beispielcode

```python
print("Markdown wird mit Pandoc nach HTML konvertiert.")
```
